You can make changes to the driver.py files to modify the problem to your liking. 
In the map, the '#' are obstacles and ' ' signifies traversable space.

You can then run driver.py. It should run just fine. 

I would reccommend running inside an IDE
If you would like to run inside a terminal,
1. cd into this folder
2. python3 driver.py

